import React from 'react';
import PropTypes from 'prop-types';
import './style.scss';

Loading.propTypes = {

};

function Loading(props) {
    return (
        <div>
            Loading
        </div>
    );
}

export default Loading;